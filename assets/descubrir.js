// Asegúrate de que el DOM esté completamente cargado antes de ejecutar el script
document.addEventListener('DOMContentLoaded', function() {
    // Selecciona el enlace
    var link = document.querySelector('.futuristic-link');

    // Añade una clase adicional para activar el diseño futurista si es necesario
    link.classList.add('active');
});